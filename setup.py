#!/usr/bin/env python

from distutils.core import setup

setup(name='network_drift',
      version='1.0',
      description='Python Distribution Utilities',
      packages=['networkdrift.demography', 'networkdrift.utils'],
     )
